def add_array_lengths(array1, array2)
  sum = array1.length + array2.length
  return sum
end

def sum_array

  .sum()

  sum = 0
array.each { |a| sum+=a }

for index_value in array1 do
  sum = index_value
  return sum
end
